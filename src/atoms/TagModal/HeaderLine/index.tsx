import React from 'react';
import styled from 'styled-components/native';

const HeaderLineStyle = styled.View`
    border-bottom-color: #bdbdbd;
    border-bottom-width: 1px;
    margin: 0 16px;
`;

const HeaderLine = () => {
    return <HeaderLineStyle />;
};

export default HeaderLine;
